# create your own .env (never commit it)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5

# only your site is allowed by the browser to hit your API
ALLOWED_ORIGINS=http://localhost:5173

PORT=8787
NODE_ENV=development
